// Lookup Types and Lookup values

// Person Type
export enum UserType {
    STUDENT = 'student',
    TEACHER = 'teacher',
    PARENT = 'parent',
    ADMIN = 'admin'
  }

// Person Gender
  export enum GENDER {
    MALE = 'male',
    FEMALE = 'female',
    OTHER = 'other',
  }

// Person Title


// Person Hair Color

// Student Fee status

// Person Salutation

// Person Eye Color

// Personal Relationship

// Address Type